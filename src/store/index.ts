import Vue from "vue";
import Vuex from "vuex";
import MODAL_STORE from './modal';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    MODAL_STORE
  }
});
